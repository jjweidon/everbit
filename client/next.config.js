/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true,
  output: 'standalone',
  assetPrefix: '/',
  basePath: '',
};

module.exports = nextConfig;
